{
  "name": "audio_app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon server.js",
    "peer": "peerjs --port 443 --key peerjs --path /myapp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.21.0",
    "nodemon": "^3.1.4",
    "peer": "^1.0.2",
    "peerjs": "^1.5.4"
  }
}
